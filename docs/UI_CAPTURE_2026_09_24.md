# 2026-09-24 首页刷新、统计与图片记账

> 最新验收修复：坐标使用整齐间隔且柱高允许80%–95%；图片使用结构化事件读取＋本地左右/配对判断，发送后直接生成草稿，待支付不拦截。以下更早的固定90%和图片二次AI解析说明已被替代。

本轮用户明确授权：首页下拉刷新信箱、单张大卡片分类设置页、删除初次见面声音设置、统计柱图/日期/收支颜色/占比、悬浮窗截图识别、聊天选图识别。语音不再继续，版本暂保持 0.5.6，本地 release 试用，不自动发布 GitHub。

## 行为

- 公告原来每进程冷启动联网一次，前台每分钟仅更新本地有效期，不是联网轮询。新增首页下拉刷新；刷新更新内容但不安排第二次自动弹窗，失败保留缓存并提示，远端空列表可撤下公告。
- 设置采用单张大卡片，上方日常/提醒/数据/关于切换分类；保留全部原有数据/提醒配置，移除声音与重看初次见面入口。
- 统计使用固定金额轴、44dp 日期间距、24dp 柱宽，横滑浏览；日期按钮在每日收支右侧。选中使用紫色、背景轨道、渐变强调和文字，不只靠色差。全局收入绿、支出珊瑚红并保留正负号。分类占比和预算使用占比常态展示。
- 聊天「添加账单图片」调用系统照片选择器，无全相册权限。预览后点击「识别并填入」才调用 DeepSeek 图片接口；文字可改，手动发送后进入原草稿确认流程。图片不会自动入账，不保存为账单永久附件；取消或成功识别后清理当前缓存图片。
- 设置→悬浮记账开启阿噜浮球，需要系统悬浮权限。拖动移动，点击请求单次截屏授权，长按或通知关闭。截图回到聊天预览，与选图复用同一识别链路。系统限制截屏的页面可能是黑屏，不能绕过。未做常驻后台录屏、后台自动上传。

## 维护入口

- `data/announcement/AnnouncementRepository.kt`：刷新互斥、缓存和弹窗会话。
- `ui/stats/charts/CashFlowBarChart.kt`：横滑坐标图；`StatsViewModel.kt` 预算百分比。
- `ui/capture/ImageBillImport.kt`：有界图片读取、EXIF 方向、压缩和可取消网络请求。图片解释使用独立提示，不改变普通聊天缓存前缀。
- `ui/capture/ImageAttachment.kt`：照片选择、预览、失败重试、取消与文字回填。
- `ui/capture/FloatingCaptureService.kt`：用户开启的前台悬浮服务；`CapturePermissionActivity.kt` 使用独立透明任务，避免授权返回后截到本 App。
- `ui/capture/ScreenCaptureService.kt`：mediaProjection 前台服务、一次帧截取、12秒超时、释放资源。授权令牌不保存不复用。

## 验证与限制

仅运行公告手动刷新回归和本轮统计/单卡片设置 UI 用例；不重跑全套。release 必要构建及 lintVital。使用人工生成的英文小票直连 DeepSeek，成功识别支出9元及2026-09-24 12:30，不使用用户真实截图作自动网络测试。悬浮窗的系统授权、跨应用截图及 vivo 兼容性仍需用户真机手动确认，不能把编译成功记作真机验收。

参考：
- https://api-docs.deepseek.com/guides/vision/
- https://developer.android.com/media/grow/media-projection
- https://developers.google.com/chart/interactive/docs/gallery/barchart

## 餐次补记（追加需求）

按用户原话推断，不接受模型凭空编日期：早餐/早饭在10:30后暂记08:00，午餐/午饭在14:30后暂记12:00，晚餐/晚饭在21:00后暂记18:00。餐次窗口内保留默认此刻；明确“刚吃/现在”及明确钟点优先。明确昨天/其他日期按对应日餐次处理。排除午餐肉、早餐机等商品名；草稿显示“按餐次暂记，可以修改”。没有餐次或时间的话保持原默认规则。测试覆盖当前午餐、补记午餐、明确钟点、昨天早餐、午餐肉及晚餐未到。

自动 UI 截图的一次滚动用例陷入 Compose 测试滚动循环，已仅结束独立 Gradle Test Executor 7，没有结束 Kotlin/Gradle 守护进程。改为无滚动的局部组件预览，已通过；不要把那次中断写成应用运行失败，也不要继续重跑完整滚动套件。

## 识图回填修复与柱高（后续验收反馈）

- 最大柱占纵轴90%，不再按数量级粗取整导致2000只占4000轴的一半；0.01元等小值同样适用，空图保留安全轴。
- 图片输出固定一笔一行。草稿日期按原行的金额+收支方向唯一关联，保留整行；不依赖模型逐字复述商家名，不按列表位置盲配。若同金额同方向仍无法唯一对应，继续要求核对。
- 微信转账原卡与收款确认只计同一笔，从截图持有者角度区分收入/支出。状态栏时间不覆盖历史转账。最近聊天分隔线只有时分时，保留时分，日期待确认，不跨过它套用更早的日期。
- 订单仅有状态栏时间时明确输出“截图参考时间”，本地预填时分、日期暂放今天并阻止未核对日期的确认；不把截图时刻声称为支付完成时刻。待支付状态保留。
- 用户原图直连验证：微信输出两笔收入，完整日期时分和仅时分分别保留；订单读出16:41及待支付。视觉模型仍可能漏读，不能保证任意图片无误。只跑日期关联/参考时间及柱高相关测试，未重跑整套UI测试。旧历史与已入账账单不自动改写，需删除错误草稿后重新识图。

## 结构化识图与草稿直达

用户再次反馈转账方向颠倒、重复，以及待支付图片不出草稿。现在 `core/ai/ImageReceiptCodec.kt` 定义独立JSON识图协议：模型只逐个读取时间行、转账卡的left/right与状态原文、金额和订单字段，不自行合并或推断收支。本地按发送卡/收款确认卡的左右组合判方向，只合并相邻的同金额同对方互补卡；不同时间分隔或独立同额交易不会全局去重。

输出带“【图片记账】”标识，仍可编辑。`AiRepository.parse` 对该格式直接本地生成add草稿，不调用第二次AI。每笔保留自己的timeExpression，待支付写入备注但正常生成草稿；入账依然只通过原确认按钮。编辑坏格式时提示且保留输入内容，不退回闲聊。图片读取上限改为4096边长/800万像素，避免1080×2400截图被直接降到540×1200。

柱图从常用整齐步长中选3–6段刻度，柱高允许80%–95%；2000对应上限2200、间隔550，外观样式不变。

验证仅7个相关单测＋release构建，全部通过。两张原图各直连一次：微信读到4张卡正确的left/right和状态，本地配对规则得到2笔收入；炸鸡读到待支付17.85及状态栏16:41，草稿解析不再依赖模型是否愿意记账。未跑全套UI测试。

## 转账分类与时间一次确认

识图协议和可编辑识图文字不再输出分类字段；旧六字段文字仍能读取，但其“其他”不再覆盖转账分类。分类在生成账本草稿时处理：转账按“转账”归类，已有则复用，不存在则草稿标记新分类，只有确认入账的事务中才创建。订单复用当前账本关键词分类规则，套餐先匹配主商品再匹配附带项。

`DraftUi.requiresTimeInput` 仅在 timeNeedsReview 且 timestamp 为空时拦住确认。若已有显示的建议日期/时间，整张草稿的确认按钮即可接受，不再强制进出时间选择器；确认后清除复核标记，持久化接受的时间。旧草稿同样适用。缺失时间和无效金额继续拦截，存储层也校验缺失时间。

本轮只执行 ImageReceiptCodecTest 与 DraftHistoryCodecTest 及 release 构建，不做额外网络识图和整套界面回归。

## 悬浮开关记忆与会话内截屏复用

用户反馈每张图弹系统授权且清后台后开关忘记。开关现在单独保存在 UserPrefs：进程退出/被清理不改偏好；MainActivity RESUMED 时在已有悬浮权限下恢复浮球。用户在设置关闭、长按关闭、悬浮通知关闭才保存false。两个服务stopWithTask且非sticky，用户移除任务时停止，不在后台自启。

首次主动开启浮球后申请截屏授权（只准备，不截图）。ScreenCaptureService 每次授权只创建一个 VirtualDisplay，整个有效会话复用；每次点击附加新的 ImageReader，截图后断开surface并关闭reader，闲置不接收/保存屏幕帧。保留系统共享指示和前台通知，通知可结束会话。授权令牌仅留在活动对象，不落盘、不跨进程复用。系统onStop、进程被清理后失效，下次点击走新的系统授权。重新进App只恢复浮球，不自动弹屏幕授权。Android14+请求整屏用于跨应用截图。

只验证偏好写入/重建读取/主动关闭及必要release构建；Android厂商实际会话保持、系统取消和连续截屏仍需真机验收，不能以单元测试冒充。官方约束：https://developer.android.com/media/grow/media-projection

## 按日浏览、分类抽屉与临时隐藏浮球

最新交互：长按浮球出现底部奶油紫关闭区，拖入后高亮并提示松开；松手暂时隐藏浮球，功能开关不改。当前任务回到前台不恢复；新的启动任务或进程重启清除本次隐藏状态，按已保存开关恢复。设置里主动关开才改变持久偏好；浮球通知关闭同样只临时隐藏。

首页月汇总保留，下面改为单日账单。进入首页、从统计返回、App恢复到首页时默认今天。左右滑动/左右箭头切日期，点日期跳日历，支持跨月历史，不只查询当月。每日收入支出分别汇总；收支筛选和排序各一个紧凑菜单。

统计柱图的选中日与收支类型统一控制分布饼图、分类百分比、明细。饼图可左右滑动换日，也同步切换柱图选中日/月。分类项下面内嵌展开明细抽屉，自动带入可视范围，账单点击仍可改；无选中分类不再独立展示当天全部账单。“其余”合并项可展开其包含账单。预算使用自己的原周期，不跟随这些筛选。

维护入口：DayBrowsing.kt 提供日期导航、横滑和小菜单；HomeViewModel.dailyBills 按本地日期区间查询，StatsViewModel统一收支/日期/分类流；临时隐藏只放FloatingCaptureService.hiddenForSession，不写UserPrefs。

本轮验证：5项日期/筛选数据测试与1项首页、内嵌抽屉界面测试通过。只检查本轮改动，release构建通过；浮球拖入关闭区及厂商清后台行为仍需手机实际操作验收。
